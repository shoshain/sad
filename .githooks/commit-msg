#!/bin/sh
# Final pass: strip Cursor co-author trailers before the commit is recorded.
MSG_FILE="${1:?}"
[ -f "$MSG_FILE" ] || exit 0
tmp="$(mktemp)"
grep -viE 'co-authored-by:.*cursoragent@cursor\.com' "$MSG_FILE" >"$tmp" || true
mv "$tmp" "$MSG_FILE"
